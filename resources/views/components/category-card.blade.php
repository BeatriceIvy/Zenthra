<div class="category-card">
    <div class="tengah">
        <img src="{{ asset('assets/image/category/' .$category->image) }}" alt="{{ $name }}" class="card-img">
    </div>
    <div class="row tengah cta">
        <h1 class="head">{{ $name }}</h1>
        {{-- <button class="btn-category"><i class="fa-solid fa-arrow-right"></i></button> --}}
    </div>
</div>